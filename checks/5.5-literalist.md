# Challenge 5.5: Literalist Objections

Objective: Audit `git diff --stat` and working tree state against the In-scope and Out-of-scope sections of the Plan.

1. **Unlisted challenge checks directory in top-level in-scope list**
   - Path: `checks/` (`checks/3.5-falsifier.md`, `checks/3.6-literalist.md`, `checks/5.4-successor.md`, `checks/5.5-literalist.md`)
   - Objection: The In-scope section explicitly lists `stream_server.py`, `static/index.html`, `tests/`, `README.md`, `camerastream.service`, and `DEPLOY.md`. While Challenge steps 3.5, 3.6, 5.4, and 5.5 explicitly mandate creating files in `checks/`, the top-level In-scope bullet list omitted `checks/`.

2. **Unlisted test baseline directory in git ignore list**
   - Path: `tests/baseline/` (`tests/baseline/index_baseline.html`, `tests/baseline/index_baseline_headers.txt`)
   - Objection: Step 1.4 created `tests/baseline/` to store Phase 1 response captures, and Step 1.6 added `tests/baseline/` to `.gitignore`. The top-level In-scope list names `tests/` generally but does not explicitly list the `tests/baseline/` ignored subfolder.

3. **Absence of unauthorized changes to out-of-scope launcher scripts**
   - Paths: `open_vlc_stream.bat`, `record_stream.bat`, `record_and_preview.bat`
   - Objection Verification: The Out-of-scope section strictly prohibits touching PC-side `.bat` launcher files. `git diff --stat` confirms zero modifications to `.bat` files, fully adhering to out-of-scope boundaries.
